<h1 class="green">My Rails Girls Diary</h1>
	<div>
	  <% for entry in @entries %>
	  	<h2><%= link_to(entry.title, entry_path(entry)) %></h2>
	  		<p><%= entry.date %></p>
	  		<p><%= entry.contents %></p>
	  <% end %>
	  <%= link_to("New entry", new_entry_path) %>
  	</div>

